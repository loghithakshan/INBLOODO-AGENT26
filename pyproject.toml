[project]
name = "inbloodo-agent"
version = "2.0.0"
requires-python = ">=3.11"
description = "FastAPI blood report analysis service"

[project.scripts]
# Lets Vercel resolve the FastAPI app explicitly if needed.
app = "app:app"
